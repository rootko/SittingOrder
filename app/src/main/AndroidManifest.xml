<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sk.halmi.sittingorder">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity_tono"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <activity
            android:name=".Editujmiestnost"
            android:label="@string/edit_room"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".VyhladajZamestnanca"
            android:label="@string/title_search_person"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Vysledkyvyhladavania"
            android:label="@string/TV_textVysledok"
            android:theme="@style/AppTheme" />
        <activity android:name=".Splashactivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>